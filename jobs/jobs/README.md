# Validation artifacts

- `oracle-pass/`: Harbor run with `-a oracle`, reward 1.
- `nop-fail/`: Harbor run with `--agent nop`, reward 0.

These captured verifier logs show the deterministic pass and fail behavior. Rerun the task after any task change to produce fresh CTRF output.
