# Harbor log-report task

A corrected Harbor task that analyzes `/app/access.log` and writes `/app/report.json`.

The repository includes the fixed task source and `jobs/` artifacts from validation runs.
